# Our releases are tagged skills-v<version>, not v<version>.
# Upstream's 41 v1.x tags are ancestors of our history and would otherwise
# collide with (and hijack `git describe` from) our own release tags.
# See docs/fork-strategy.md#releasing--tagging
tag-version-prefix=skills-v
